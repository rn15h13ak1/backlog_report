# Changelog

[Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) 形式に概ね準拠し、
[セマンティックバージョニング](https://semver.org/lang/ja/) に従います。

バージョンはコミット履歴からさかのぼって付けたもので、タグは打っていません。

## [Unreleased]

### Added
- docmold の `weekly3` に渡す Markdown を `weekly3_report.md` として出力する。
  前週・今週・来週の予定を 3 列に並べ、分類（フィルター）ごとに横並びで見比べられる。
  これまでの `summary_report.md`（Excel 貼り付け用）はそのまま残る
  - 真ん中の列が今回の集計、左が前回の集計（`snapshot.json` から復元）、
    右は ⑤ をそのまま持ち越した「来週の予定」
  - 来週の予定では、期限が次の期間の開始日より前の課題のステータスを「期限超過」に
    差し替える。docmold 側で状態バッジになる
  - 列の見出しには期間を書き込む。docmold の `column_periods` は見出しに区切り（〜）が
    あれば触らないため、7 日以外の期間でも正しい範囲が出る
  - トピックスの列に、分類ごとの件数表と、出入り・取得失敗などの注意書きを載せる

### Changed
- `snapshot.json` に、フィルターごとの件数と ④ 完了の課題を追加した。次回の
  weekly3 レポートで「前週」の列を組み立てるために使う。読む側は欠けていても動くので、
  以前の形式のスナップショットもそのまま使える

## [0.5.0] - 2026-09-14

週をまたいだ照合（前週⑤ = 今週①）が成立するようにしました。

### Added
- 期間フォルダに `snapshot.json` を出力する。フィルターごとの ⑤ と絞り込み条件を
  記録し、次回の実行で読む。課題番号・件名・ステータス・期限・担当者を持たせてあるので、
  次回、抽出対象から外れた課題を表示する際に再取得しなくてよい
- 抽出対象への出入りを集計に反映する。種別やカスタム属性を変更すると課題が
  抽出対象に入ったり外れたりするため、そのままでは前週の ⑤ と今週の ① が合わない
  - 流入（前回 ⑤ に無いのに今回 ① に居る）は期首の在庫ではないので ① から ② へ移す
  - 流出（前回 ⑤ に居たのに今回の母集団に無い）は期首は在庫なので ① に含めたうえで、
    期中に対応不要になったものとして ④ にも含める
  - 課題の属性そのものは見ず、前回 ⑤ との集合の差分だけで判定する。このため種別・
    カスタム属性・件名キーワードのどれで絞っていても同じように動き、API の追加取得もない
- `closed_status_ids` の検証。プロジェクトの先頭ステータス（新規作成時のステータス。
  既定では「未対応」）を完了系に登録すると、すべての課題が作成時点で完了していた
  扱いになり集計が成り立たないため、実行前にエラーで停止する
- 判定できない場合（初回実行、週を飛ばした、出力先を変えた、絞り込み条件を書き換えた、
  スナップショットが壊れている）は理由をレポートに注記する。このとき ① と ② の内訳は
  実態と異なるが、⑤ は影響を受けないため次回以降の照合は正しく行える

### Changed
- 完了系に登録されていないステータスを、すべてオープン系として扱う。これまでは
  `open_status_ids` と `closed_status_ids` のどちらにも無いステータスが ①③④ の判定を
  すり抜け、集計から漏れていた。指定するのは完了系だけになり、`open_status_ids` は
  不要になったため削除した
- ④ 当週完了に「期間終了時点も完了系であること」を条件として追加。期間中に完了して
  同じ期間中に再オープンされた課題は、期末時点では未完了なので ⑤ に入るべきだった。
  これまでは ④ に入って ⑤ から外れるため、翌週 ① に現れて件数が合わなくなっていた
- 「ステータス一覧に無い名前」の警告を、changeLog にプロジェクトの現在のステータス
  一覧に存在しない名前が現れた場合（改名・削除の可能性）に変更した
- `run()` のフィルター有無による分岐を統合した。中身は同じ流れだったため、
  フィルターなしも「1 件の仕事」として扱う。サマリー直下の注記は 5 種類に増えて
  本体を圧迫していたので別の関数へ分離した

### Fixed
- ① 前週残件のステータスを常に期間開始時点で表示する。これまでは ④ にも入る場合だけ
  差し替えており、期間より後に変化した課題は現在のステータスが表示されていた。
  同じ課題が ① と ⑤ で違うステータスに見えていた
- 終了日が同じ期間フォルダが複数ある場合の選択を決定的にした。フォルダの列挙順は
  ファイルシステム依存のため、`3/1〜3/8` と `3/2〜3/8` の両方で実行した状態から
  `3/9` 開始の集計を行うと、読むスナップショットが実行のたびに変わりうる状態だった。
  開始日が最も遅いもの（直近の集計）を選び、複数見つかったことを警告する

## [0.4.0] - 2026-08-29

日付判定のずれを直し、実行速度とテストを整えました。

### Added
- `pyproject.toml`（依存関係・pytest・ruff・カバレッジの設定）。依存は README の
  散文にしか書かれておらず、Lint とテストの設定も無かった
- テスト一式。期間の決定、フィルター条件の解決、ステータス分類、API クライアント、
  レポート出力の形式（保存した出力との全文比較）、設定の読み込みから書き出しまでの
  通し実行
- 旧実装との差分テスト。実 API に接続できない環境で回帰を検出するため、変更前の
  実装を `tests/_legacy.py` に凍結し、合成データで新旧の結果を突き合わせる
- コメント取得の並列数を `report.max_workers` で調整できるようにした

### Changed
- 日付の判定をすべて JST（UTC+9）基準にした。Backlog API は日時を UTC で返すが、
  ISO 文字列の先頭 10 文字を切ってローカル日付と比較していたため、期間の境界で
  9 時間ぶんのずれが出ていた
- 集計に寄与しない課題を取得しない。「現在のステータスが完了系」かつ「期間開始以降に
  更新なし」の課題は ①〜⑤ のどれにも入らないため、取得を「完了系でない課題」と
  「期間開始以降に更新された課題」の和集合に絞った。プロジェクトの課題数が増えても
  取得件数はほぼ増えない
- 期間開始以降に更新されていない課題は、コメントを取得しない。同じ課題が複数の
  フィルターに現れても取得は 1 回だけにし、ステータス一覧もプロジェクトごとに
  1 回だけにした
- 日時変換を高速化した。課題 1 件あたりコメント数ぶん呼ばれ、フィルターごとに同じ値を
  変換し直すため処理時間に効いていた。固定長の形式は文字列のまま判定し、結果を
  キャッシュする
- `check_api.py` を `BacklogClient` を利用する実装に書き換えた。SSL 設定・クエリ
  組み立て・設定読み込みが本体と重複しており、`config.yaml` を相対パスで開いていた

### Fixed
- API 呼び出しの失敗で処理全体が止まらないようにした。`_get` が `sys.exit` していたため
  呼び出し側の例外処理が機能せず、コメント取得 1 回の失敗で数分の処理が失われていた。
  例外を送出する形に改め、429 / 5xx / 接続エラーは指数バックオフで最大 3 回リトライする
- リトライの待ち時間にばらつき（0〜50% の上乗せ）を加えた。並列で動くワーカーが
  同時にレート制限に掛かると、全員が同じ秒数だけ待って同時に再送し、また衝突していた。
  サーバーが `Retry-After` で指示した秒数は下回らない
- `max_workers` を 1〜8 に収める。設定に大きな値を書くとそのまま並列数になっていた
- 完了系・オープン系のステータス ID の既定値が 3 ファイルで食い違っていた
- `pip install -e ".[dev]"` が失敗していた（トップレベルのモジュールが 2 つあり、
  setuptools が対象を自動判別できなかった）
- `get_issues` が呼び出し元の辞書を書き換えていた

## [0.3.0] - 2026-04-09

複数フィルターの指定を広げ、横断サマリーを追加しました。

### Added
- フィルターごとに `project_key` を指定できるようにした。1 回の実行で別プロジェクトも
  集計できる。同じ `project_key` が複数あっても API 呼び出しは 1 回
- フィルターに `keyword`（件名・詳細の部分一致）を追加
- 全フィルターを横断するサマリーレポート（`summary_report.md`）。件数と、完了・未完了の
  課題を課題番号順に並べる。Excel へコピー＆ペーストして使う想定の形式
- レポートの出力先を期間フォルダ（`YYYYMMDD_YYYYMMDD`）配下に変更

### Changed
- カテゴリ番号を ①前週残件 ②新規発生 ③再オープン ④当週完了 ⑤当週未完了 に統一
- ②③④⑤ の表示ステータスを期間終了時点に統一した。レポート実行時点の値が
  混ざらないようにするため

### Fixed
- ④ 当週完了の判定をオープン系 → 完了系の変化のみに限定した。完了系 → 完了系
  （処理済み → 完了 など）を完了として数えていた
- 期間より後にのみステータスが変化した課題の、期間終了時点のステータスを正しく
  導出するようにした。期間後の最初の変化の「変更前」が、その期間中の状態にあたる
- サマリーレポートの課題番号を数値順に並べた（文字列順では `PRJ-10` が `PRJ-2` より
  前に来ていた）

## [0.2.0] - 2026-03-27

集計の土台を、現在のステータスに依存しない方式へ全面的に作り替えました。

### Added
- ③ 再オープンを独立したカテゴリとして集計する。期間開始時点で完了系だった課題が
  期間中にオープン系へ戻ったもの
- ①＋②＋③ = ④＋⑤ が成立しない場合に、レポートのサマリー直下へ警告を出す
- API 接続の診断スクリプト `check_api.py`。接続・認証、プロジェクト、ステータス一覧、
  コメントの changeLog をまとめて確認する

### Changed
- ステータス変化の判定を、プロジェクトのアクティビティ走査から各課題のコメント履歴
  （changeLog）へ全面的に書き換えた。現在のステータスに依存せず、期間開始時点と
  期間終了時点のステータスを履歴から復元して、過去の期間を正確に集計する
- `config.yaml` を `config.sample.yaml` に改名し、`config.yaml` を `.gitignore` に
  追加した（API キーを含むため）
- サマリーの並びを 残件 / 新規 / 再オープン / 完了 / 未完了 に統一

### Fixed
- ① 前週残件の表示ステータスを期間開始時点のものに変更
- 再オープンした課題が期間後に再び完了していた場合の取りこぼし

## [0.1.0] - 2026-03-25

最初の実装。

### Added
- Backlog REST API から課題を取得し、指定期間の集計を Markdown として出力する本体
- 種別・カスタム属性による複数フィルター。フィルターごとに個別のレポートを生成する
- 期間の指定方法を 4 通り用意し、`--from/--to` > `--week` > `config.yaml` の `period` >
  `target_week` の優先順位で決める
- 週の開始曜日を任意に指定できる（英語・日本語の両方の表記に対応）
- オンプレミス版への対応。パスプレフィックス（`base_path`）と、自己署名証明書の
  環境向けに SSL 検証の無効化（`ssl_verify`）
- `--debug` で API リクエストのパラメータを表示する（API キーは出力しない）
- 課題の詳細テーブルに期限日の列
- HTTP ステータスごとの診断メッセージ。400 の場合はレスポンス本文も表示する

### Fixed
- 配列パラメータの角括弧をパーセントエンコードする（`statusId[]` → `statusId%5B%5D`）。
  RFC3986 でクエリ文字として非合法なため
- リスト型カスタム属性の値を、選択肢名から数値 ID に変換してから送る
- `config.yaml` と出力先の相対パスを、実行位置ではなくスクリプトの場所を基準に
  解決する（フルパスでの実行に対応）
- 完了系・オープン系のステータス ID の既定値を `[1, 2]` / `[3, 4]` に修正
